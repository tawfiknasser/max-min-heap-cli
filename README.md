
# Max-Min-Heap CLI

Time Complixity:

          Build_Heap             O(N)
          
          Heapify
          
          Heap_Insert            O(Log(N))
          
          Heap_Delete            
          
          Heap_Extract_Min       O(Log(N))
          
          Heap_Extract_Max       O(Log(N))
          
          Heap_Sort              O(N*Log(N))

